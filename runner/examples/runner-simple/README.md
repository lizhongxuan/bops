# runner-simple

Run a workflow YAML using the extracted runner library.

```bash
# from repo root

go run ./runner/examples/runner-simple ./examples/simple.yaml
```

You can also pass any YAML you created:

```bash
go run ./runner/examples/runner-simple /path/to/workflow.yaml
```
